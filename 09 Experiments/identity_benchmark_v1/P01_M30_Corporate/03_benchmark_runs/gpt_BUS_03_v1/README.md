# P01 GPT BUS_03 v1 — provisional run

This is the second GPT exploratory benchmark run for P01 v1.0, covering `BUS_03 — Executive 3/4`. It contains C01–C03 and uses the same InsightFace `buffalo_l` CPU methodology as `gpt_BUS_01_v1`.

The run is **provisional**. It was generated interactively in ChatGPT; canonical P01 references were present in the conversation context, but strict proof that only REF01–REF03 were inputs is unavailable. It supports exploratory analysis only and is not strict cross-model benchmark evidence.

Machine mean identity similarity is C01 `0.880694`, C02 `0.853416`, C03 `0.843049`; all candidates are readable and have one detected face. No P01-calibrated threshold exists, so all machine decisions are `REVIEW` and human review remains pending.

The comparison artifact reports BUS_03 mean `0.859053` versus BUS_01 mean `0.875246` (delta `-0.016193`). This is descriptive only; two scenes with provisional provenance do not establish a provider or backend conclusion. Candidate PNG remain local generated artifacts.
